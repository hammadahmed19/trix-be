{
  "type": "module",
   "scripts": {
    "dev": "node src/app.js", // Start the app using Express (adjust path to your app file)
    "start": "node src/app.js" // For production use; runs the same as dev in this case
  },
  "dependencies": {
    "express": "^4.22.1",
    "mongoose": "^8.20.4",
    "dotenv": "^16.6.1",
    "cors": "^2.8.5"
  },
  "engines": {
    "node": "24.x"
  }
}
